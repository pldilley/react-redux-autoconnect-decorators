{
  "name": "react-redux-autoconnect-decorators",
  "version": "1.0.0",
  "description": "Decorators to simplify react-redux's `connect()`",
  "main": "./lib/reduxDecorators.js",
  "author": "Paul Dilley",
  "license": "MIT",
  "scripts": {
    "build": "build src -d lib --presets=babel-preset-env",
    "compile": "babel src -d lib",
    "test": "jest",
    "prepublish": "npm run compile"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^20.0.4"
  },
  "peerDependencies": {
    "react-redux": ">=3.0.0"
  }
}
